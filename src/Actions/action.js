export const addQuery = query => ({
  type: "addQuery",
  query
});

export const changedQuery = query => ({
  type: "changedQuery",
  query
});

export const loadQuery = query => ({
  type: "loadQuery",
  query
});
